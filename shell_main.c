#include "shell.h"

/**
 * main - Entry point
 * @ac: arg count
 * @av: arg vector
 *
 * Return: 0 on Success, 1 on error
 */
int main(int ac, char **av)
{
	data_shell a[] = {ARGS_INIT};

	prompt();
	return (0);
}
